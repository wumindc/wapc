# WAPC 项目全局开发规范 (Agents.md)

本文档定义了 WAPC 平台在进行后续开发和维护时，AI 代理或开发人员必须遵守的全局规则和开发范式。

## 1. 核心前端界面要求

- **全面自适应与响应式 (Responsive Design)**：
  - 所有的页面功能和组件必须原生支持响应式布局（Mobile, Tablet, Desktop）。
  - 在不同尺寸下必须妥善处理侧边栏、工具栏和数据表格（如使用横向滚动或堆叠卡片）。
  - 数据面板和列表必须能在小屏幕下提供合理的降级展现方式，不可出现固定宽度导致的溢出或遮挡。
- **亮暗色模式双轨支持 (Dark/Light Mode)**：
  - 全局强制支持亮色（Light Mode）与暗色（Dark Mode）模式。
  - 必须使用标准的 Tailwind CSS 变量约定来绑定主题（如使用基于 `.dark` class 的 `bg-surface`、`text-muted` 等变量映射），严禁在组件内部硬编码纯黑或纯白色。
- **基础动画与平滑交互 (Animations & Transitions)**：
  - 所有页面的切换必须包含基础的过渡动画（如全局的 `.fade-in`）。
  - 所有按钮、卡片在悬浮 (hover) 时必须带有平滑的色彩或阴影渐变（建议使用 `.transition-base` 及 `transition-colors` 等）。
  - 数据加载和列表操作需带有基础的视觉反馈，严禁生硬的状态突变。

## 2. 技术栈与框架规范

- **前端技术栈**：强制使用 React + TypeScript 编写，依赖管理强制使用 `yarn`。
- **UI 库约束**：优先考虑使用 `lucide-react` 作为图标库，并基于 `TailwindCSS` v4 构建样式系统，不引入臃肿的第三方重量级 UI 库。
- **HTML 元素约束**：编写列表（如 Table、Grid 等）时，列的 width 默认不要通过硬编码设置，交由浏览器的 Flex 或 Grid 引擎自动计算适应。
- **质量校验**：进行任何前端代码变更后，必须主动执行 `yarn build` 等构建命令验证页面功能和渲染情况，确保无报错后方可交付。

## 3. 工程化与代码结构

- **组件级拆分**：每个功能页面尽可能拆分成多个独立的 `.tsx` 文件组件，通过 `import` 的方式组合。避免将上千行代码堆砌在一个文件中。
- **视图与数据逻辑解耦**：
  - UI 视图文件应尽量保持纯粹的呈现逻辑。
  - 数据获取和业务计算必须抽离到独立的 hooks 文件中处理（例如 `useMcpData.ts`）。
  - 编写静态模拟数据时，可将其统一包装到 `mock-hooks.ts` 中，真实的服务端接口（Tauri API 等）调用逻辑写在正式的 `hooks.ts` 中，保持环境隔离。

## 4. 文档与变更追踪

- **变更日志**：每次功能变更必须将变更信息登记到工程根目录下的 `CHANGELOG.md` 中，清楚记录：**变更内容**、**操作时间**、**变更需求/目标**。
- **文档沉淀**：所有方案设计和系统说明文档统一存储在 `docs` 目录中。
  - 格式必须为 Markdown (`.md`)。
  - 命名规范要求为 `YYYYMMDD-序列号-文档标题.md`（例如：`20251230-001-RBAC方案设计.md`）。
- **语言**：在代码注释和沟通中，必须默认使用中文，并在特殊复杂逻辑的注释中增加 `@author` 标记标明当前使用的工具或 AI 模型。
