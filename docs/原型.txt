import React, { useState, useEffect } from 'react';
import {
  PieChart,
  BarChart,
  LineChart,
  Settings,
  Menu,
  X,
  Server,
  Code,
  Box,
  Monitor,
  Download,
  Upload,
  ChevronDown,
  ChevronRight,
  Activity,
  CheckCircle,
  AlertCircle,
  HelpCircle,
  Search,
  Filter
} from 'lucide-react';

// --- MOCK DATA ---
const mockTools = [
  { id: 'claude', name: 'Claude Code', status: 'connected', type: 'CLI' },
  { id: 'gemini', name: 'Gemini CLI', status: 'connected', type: 'CLI' },
  { id: 'cursor', name: 'Cursor', status: 'manual', type: 'Editor' },
  { id: 'trae', name: 'Trae', status: 'planning', type: 'Editor' },
];

const mockMCPs = [
  { id: 'mcp1', name: 'FileSystem MCP', status: 'normal', tools: ['claude', 'cursor'], desc: '本地文件系统读写权限' },
  { id: 'mcp2', name: 'GitHub MCP', status: 'partial', tools: ['claude'], desc: 'GitHub API 交互支持' },
  { id: 'mcp3', name: 'DB Tools', status: 'error', tools: [], desc: '数据库查询与修改' },
];

const mockSkills = [
  { id: 'sk1', name: 'React Component', scope: 'Project', source: 'Claude', size: '2KB' },
  { id: 'sk2', name: 'API Error Handler', scope: 'User', source: 'Gemini', size: '1.5KB' },
  { id: 'sk3', name: 'Tailwind Layouts', scope: 'Project', source: 'Cursor', size: '3KB' },
  { id: 'sk4', name: 'SQL Optimizer', scope: 'User', source: 'Local', size: '4KB' },
];

// --- COMPONENTS ---

// 1. Sidebar Component
const Sidebar = ({ isOpen, setIsOpen, activeMenu, setActiveMenu }) => {
  const [expandedGroups, setExpandedGroups] = useState({ usage: true, core: true, tools: false, sync: false });

  const toggleGroup = (group) => {
    setExpandedGroups(prev => ({ ...prev, [group]: !prev[group] }));
  };

  const menuGroups = [
    {
      id: 'usage', label: 'Usage', icon: <Activity size={18} />,
      items: [
        { id: 'overview', label: '概览 (Overview)' },
        { id: 'tokens', label: '使用明细 (Tokens)' },
        { id: 'analytics', label: '分析视图 (Analytics)' },
      ]
    },
    {
      id: 'core', label: '核心能力库', icon: <Box size={18} />,
      items: [
        { id: 'mcp', label: 'MCP 管理', icon: <Server size={16} /> },
        { id: 'skills', label: 'Skills 管理', icon: <Code size={16} /> },
        { id: 'plugins', label: 'Plugin 包', icon: <Box size={16} /> },
      ]
    },
    {
      id: 'tools', label: '支持工具与计费', icon: <Monitor size={18} />,
      items: [
        { id: 'tool-list', label: '工具状态矩阵' },
        { id: 'pricing', label: '模型价格' },
      ]
    },
    {
      id: 'sync', label: '数据同步', icon: <Settings size={18} />,
      items: [
        { id: 'export', label: '数据导出', icon: <Download size={16} /> },
        { id: 'import', label: '数据导入', icon: <Upload size={16} /> },
      ]
    }
  ];

  return (
    <div className={`fixed inset-y-0 left-0 z-50 w-64 bg-slate-900 border-r border-slate-800 transform transition-transform duration-300 ease-in-out md:translate-x-0 ${isOpen ? 'translate-x-0' : '-translate-x-full'} md:static md:flex-shrink-0 flex flex-col`}>
      <div className="flex items-center justify-between h-16 px-4 bg-slate-950 border-b border-slate-800">
        <div className="flex items-center space-x-2 text-cyan-400 font-bold text-xl">
          <Activity size={24} />
          <span>WAPC</span>
        </div>
        <button className="md:hidden text-slate-400 hover:text-white" onClick={() => setIsOpen(false)}>
          <X size={20} />
        </button>
      </div>

      <div className="flex-1 overflow-y-auto py-4 px-3 space-y-1 custom-scrollbar">
        {menuGroups.map((group) => (
          <div key={group.id} className="mb-2">
            <button
              onClick={() => toggleGroup(group.id)}
              className="w-full flex items-center justify-between px-3 py-2 text-sm font-medium text-slate-300 hover:bg-slate-800 rounded-md transition-colors"
            >
              <div className="flex items-center space-x-3">
                <span className="text-slate-400">{group.icon}</span>
                <span>{group.label}</span>
              </div>
              {expandedGroups[group.id] ? <ChevronDown size={16} /> : <ChevronRight size={16} />}
            </button>
            
            {expandedGroups[group.id] && (
              <div className="mt-1 space-y-1 pl-10">
                {group.items.map((item) => (
                  <button
                    key={item.id}
                    onClick={() => setActiveMenu(item.id)}
                    className={`w-full flex items-center space-x-2 px-3 py-2 text-sm rounded-md transition-colors ${activeMenu === item.id ? 'bg-cyan-900/30 text-cyan-400' : 'text-slate-400 hover:bg-slate-800 hover:text-slate-200'}`}
                  >
                    {item.icon && <span className="opacity-70">{item.icon}</span>}
                    <span>{item.label}</span>
                  </button>
                ))}
              </div>
            )}
          </div>
        ))}
      </div>
    </div>
  );
};

// 2. Views

// View: Overview
const OverviewView = () => (
  <div className="p-6 space-y-6 fade-in h-full overflow-y-auto custom-scrollbar">
    <div>
      <h2 className="text-2xl font-bold text-white mb-1">概览 (Overview)</h2>
      <p className="text-slate-400 text-sm">全局 Token 与费用快照</p>
    </div>

    {/* KPI Cards */}
    <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4">
      {[
        { label: '今日 Tokens', value: '142.5K', change: '+12%', color: 'text-cyan-400' },
        { label: '本周 Tokens', value: '1.2M', change: '-5%', color: 'text-blue-400' },
        { label: '总费用 (Est.)', value: '$4.52', change: '+$0.8', color: 'text-emerald-400' },
        { label: '活跃会话', value: '24', change: '↑', color: 'text-purple-400' },
      ].map((kpi, i) => (
        <div key={i} className="bg-slate-900 border border-slate-800 rounded-xl p-5 shadow-sm">
          <div className="text-slate-400 text-sm mb-2">{kpi.label}</div>
          <div className="flex items-end justify-between">
            <div className={`text-3xl font-bold ${kpi.color}`}>{kpi.value}</div>
            <div className="text-xs text-slate-500 bg-slate-800 px-2 py-1 rounded">{kpi.change}</div>
          </div>
        </div>
      ))}
    </div>

    {/* Chart Placeholder */}
    <div className="bg-slate-900 border border-slate-800 rounded-xl p-5 h-80 flex flex-col">
       <h3 className="text-white font-medium mb-4">近 7 天 Token 消耗趋势</h3>
       <div className="flex-1 border border-dashed border-slate-700 rounded-lg flex items-center justify-center text-slate-500">
          <LineChart size={48} className="opacity-20 mb-2" />
          <span>[趋势图表区域 - 需引入 Recharts 或 Echarts]</span>
       </div>
    </div>
  </div>
);

// View: MCP Management (Master-Detail)
const MCPView = () => {
  const [selectedMCP, setSelectedMCP] = useState(mockMCPs[0]);

  return (
    <div className="flex h-full fade-in">
      {/* Master List */}
      <div className="w-1/3 min-w-[300px] border-r border-slate-800 bg-slate-900/50 flex flex-col h-full">
        <div className="p-4 border-b border-slate-800">
          <h2 className="text-lg font-bold text-white mb-2">MCP 服务器</h2>
          <div className="relative">
            <Search className="absolute left-3 top-1/2 transform -translate-y-1/2 text-slate-500" size={16} />
            <input type="text" placeholder="搜索 MCP..." className="w-full bg-slate-950 border border-slate-800 rounded-md py-1.5 pl-9 pr-3 text-sm text-slate-200 focus:outline-none focus:border-cyan-500" />
          </div>
        </div>
        <div className="flex-1 overflow-y-auto custom-scrollbar">
          {mockMCPs.map(mcp => (
            <button
              key={mcp.id}
              onClick={() => setSelectedMCP(mcp)}
              className={`w-full text-left p-4 border-b border-slate-800/50 hover:bg-slate-800 transition-colors ${selectedMCP.id === mcp.id ? 'bg-slate-800 border-l-2 border-l-cyan-500' : ''}`}
            >
              <div className="flex items-center justify-between mb-1">
                <span className="font-medium text-slate-200">{mcp.name}</span>
                {mcp.status === 'normal' && <span className="w-2 h-2 rounded-full bg-emerald-500 shadow-[0_0_8px_rgba(16,185,129,0.5)]"></span>}
                {mcp.status === 'partial' && <span className="w-2 h-2 rounded-full bg-amber-500 shadow-[0_0_8px_rgba(245,158,11,0.5)]"></span>}
                {mcp.status === 'error' && <span className="w-2 h-2 rounded-full bg-red-500 shadow-[0_0_8px_rgba(239,68,68,0.5)]"></span>}
              </div>
              <p className="text-xs text-slate-500 line-clamp-1">{mcp.desc}</p>
            </button>
          ))}
        </div>
      </div>

      {/* Detail Area */}
      <div className="flex-1 bg-slate-950 p-6 overflow-y-auto">
        {selectedMCP ? (
          <div className="max-w-2xl">
            <div className="flex items-center space-x-3 mb-6">
              <div className="p-3 bg-cyan-900/20 text-cyan-400 rounded-lg">
                <Server size={24} />
              </div>
              <div>
                <h2 className="text-2xl font-bold text-white">{selectedMCP.name}</h2>
                <div className="flex items-center space-x-2 text-sm mt-1">
                   <span className={`px-2 py-0.5 rounded text-xs ${selectedMCP.status === 'normal' ? 'bg-emerald-900/30 text-emerald-400' : selectedMCP.status === 'error' ? 'bg-red-900/30 text-red-400' : 'bg-amber-900/30 text-amber-400'}`}>
                     {selectedMCP.status === 'normal' ? '运行正常' : selectedMCP.status === 'error' ? '解析异常' : '部分挂载'}
                   </span>
                   <span className="text-slate-500">ID: {selectedMCP.id}</span>
                </div>
              </div>
            </div>

            <div className="bg-slate-900 border border-slate-800 rounded-xl p-5 mb-6">
              <h3 className="text-sm font-medium text-slate-400 mb-3 uppercase tracking-wider">支持矩阵</h3>
              <div className="flex flex-wrap gap-2">
                {mockTools.map(tool => {
                  const isSupported = selectedMCP.tools.includes(tool.id);
                  return (
                    <div key={tool.id} className={`flex items-center space-x-2 px-3 py-2 rounded-md border ${isSupported ? 'bg-slate-800 border-slate-700 text-slate-200' : 'bg-slate-950 border-slate-800 text-slate-600'}`}>
                      {isSupported ? <CheckCircle size={14} className="text-emerald-500" /> : <Monitor size={14} />}
                      <span className="text-sm">{tool.name}</span>
                    </div>
                  )
                })}
              </div>
              <button className="mt-4 px-4 py-2 bg-slate-800 hover:bg-slate-700 text-cyan-400 text-sm font-medium rounded-md transition-colors">
                + 同步到其他工具
              </button>
            </div>

            <div className="bg-slate-900 border border-slate-800 rounded-xl p-5">
               <h3 className="text-sm font-medium text-slate-400 mb-3 uppercase tracking-wider">配置 JSON</h3>
               <pre className="bg-slate-950 p-4 rounded-lg border border-slate-800 text-sm text-cyan-300 overflow-x-auto font-mono">
{`{
  "mcpServers": {
    "${selectedMCP.name.toLowerCase().replace(' ', '-')}": {
      "command": "node",
      "args": ["/path/to/server.js"]
    }
  }
}`}
               </pre>
            </div>

          </div>
        ) : (
          <div className="h-full flex items-center justify-center text-slate-500">
            请在左侧选择一个 MCP 服务器查看详情
          </div>
        )}
      </div>
    </div>
  );
};

// View: Skills Gallery
const SkillsView = () => {
  const [selectedSkill, setSelectedSkill] = useState(null);

  return (
    <div className="relative h-full flex flex-col fade-in">
      <div className="p-6 pb-2 border-b border-slate-800">
        <div className="flex justify-between items-end mb-4">
          <div>
            <h2 className="text-2xl font-bold text-white mb-1">Skills 技能管理库</h2>
            <p className="text-slate-400 text-sm">Prompt 与 Agent 技能资产流转</p>
          </div>
          <div className="flex space-x-2">
            <button className="p-2 bg-slate-800 hover:bg-slate-700 rounded text-slate-300"><Filter size={18} /></button>
            <button className="px-4 py-2 bg-cyan-600 hover:bg-cyan-500 text-white rounded text-sm font-medium shadow-lg shadow-cyan-900/20">导入 Skill</button>
          </div>
        </div>
      </div>

      <div className="flex-1 p-6 overflow-y-auto">
        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-4">
          {mockSkills.map(skill => (
            <div 
              key={skill.id} 
              onClick={() => setSelectedSkill(skill)}
              className="bg-slate-900 border border-slate-800 rounded-xl p-5 hover:border-cyan-500/50 hover:shadow-lg hover:shadow-cyan-900/10 cursor-pointer transition-all group"
            >
              <div className="flex justify-between items-start mb-4">
                <div className="p-2 bg-purple-900/20 text-purple-400 rounded-lg group-hover:bg-purple-900/40 transition-colors">
                  <Code size={20} />
                </div>
                <span className={`text-xs px-2 py-1 rounded ${skill.scope === 'Project' ? 'bg-blue-900/30 text-blue-400' : 'bg-slate-800 text-slate-400'}`}>
                  {skill.scope}
                </span>
              </div>
              <h3 className="text-lg font-bold text-slate-200 mb-1">{skill.name}</h3>
              <div className="flex items-center space-x-2 text-xs text-slate-500 mt-4">
                <span className="flex items-center"><Monitor size={12} className="mr-1"/> {skill.source}</span>
                <span>•</span>
                <span>{skill.size}</span>
              </div>
            </div>
          ))}
        </div>
      </div>

      {/* Right Drawer for Detail */}
      <div className={`absolute top-0 right-0 h-full w-96 bg-slate-900 border-l border-slate-800 shadow-2xl transform transition-transform duration-300 ease-in-out flex flex-col z-10 ${selectedSkill ? 'translate-x-0' : 'translate-x-full'}`}>
         {selectedSkill && (
           <>
            <div className="flex items-center justify-between p-4 border-b border-slate-800">
              <h3 className="font-bold text-white">{selectedSkill.name}</h3>
              <button onClick={() => setSelectedSkill(null)} className="p-1 text-slate-400 hover:text-white rounded"><X size={20} /></button>
            </div>
            <div className="p-4 overflow-y-auto flex-1">
              <div className="space-y-4">
                <div>
                  <div className="text-xs text-slate-500 mb-1">来源</div>
                  <div className="text-sm text-slate-300 bg-slate-950 px-3 py-2 rounded border border-slate-800">{selectedSkill.source}</div>
                </div>
                <div>
                  <div className="text-xs text-slate-500 mb-1">Markdown 预览</div>
                  <div className="bg-slate-950 border border-slate-800 rounded-lg p-4 font-mono text-xs text-slate-400 h-64 overflow-y-auto custom-scrollbar">
                    # {selectedSkill.name}<br/><br/>
                    You are an expert in...<br/><br/>
                    ## Instructions<br/>
                    1. Always check...<br/>
                    2. Never use...<br/>
                    <br/>
                    [Preview content...]
                  </div>
                </div>
              </div>
            </div>
            <div className="p-4 border-t border-slate-800 bg-slate-950">
               <button className="w-full py-2 bg-slate-800 hover:bg-slate-700 text-cyan-400 text-sm font-medium rounded-md transition-colors flex items-center justify-center">
                 <CopyIcon className="w-4 h-4 mr-2" /> 克隆到其他工具
               </button>
            </div>
           </>
         )}
      </div>
    </div>
  );
};

// View: Tools Status
const ToolsStatusView = () => (
  <div className="p-6 space-y-6 fade-in h-full overflow-y-auto custom-scrollbar">
    <div>
      <h2 className="text-2xl font-bold text-white mb-1">支持工具矩阵与健康度</h2>
      <p className="text-slate-400 text-sm">监控本地 AI 编程工具的连接状态</p>
    </div>

    <div className="bg-slate-900 border border-slate-800 rounded-xl overflow-hidden">
      <table className="w-full text-left border-collapse">
        <thead>
          <tr className="bg-slate-950 border-b border-slate-800 text-slate-400 text-sm uppercase tracking-wider">
            <th className="p-4 font-medium">工具名</th>
            <th className="p-4 font-medium">类型</th>
            <th className="p-4 font-medium">状态</th>
            <th className="p-4 font-medium">数据源示例</th>
          </tr>
        </thead>
        <tbody className="divide-y divide-slate-800">
          {mockTools.map((tool) => (
            <tr key={tool.id} className="hover:bg-slate-800/50 transition-colors">
              <td className="p-4 font-medium text-slate-200">
                <div className="flex items-center space-x-2">
                  <Monitor size={16} className="text-slate-500" />
                  <span>{tool.name}</span>
                </div>
              </td>
              <td className="p-4 text-slate-400 text-sm">{tool.type}</td>
              <td className="p-4">
                <div className="flex items-center space-x-2">
                  {tool.status === 'connected' && <><span className="w-2 h-2 rounded-full bg-emerald-500 shadow-[0_0_8px_rgba(16,185,129,0.5)]"></span><span className="text-emerald-400 text-sm">已连接</span></>}
                  {tool.status === 'manual' && <><span className="w-2 h-2 rounded-full bg-blue-500"></span><span className="text-blue-400 text-sm">需手动配置</span></>}
                  {tool.status === 'planning' && <><span className="w-2 h-2 rounded-full bg-slate-600"></span><span className="text-slate-400 text-sm">规划支持中</span></>}
                </div>
              </td>
              <td className="p-4 text-slate-500 font-mono text-xs">
                {tool.status === 'connected' ? `~/.${tool.id}/data` : tool.status === 'manual' ? '需指定 SQLite 路径' : '—'}
              </td>
            </tr>
          ))}
        </tbody>
      </table>
    </div>
  </div>
);

// Helper Icon
const CopyIcon = ({ className }) => (
  <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" className={className}><rect width="14" height="14" x="8" y="8" rx="2" ry="2"/><path d="M4 16c-1.1 0-2-.9-2-2V4c0-1.1.9-2 2-2h10c1.1 0 2 .9 2 2"/></svg>
)

// Global Topbar Component
const GlobalTopbar = ({ selectedTool, setSelectedTool, isMobileMenuOpen, setIsMobileMenuOpen }) => {
  const [isDropdownOpen, setIsDropdownOpen] = useState(false);
  
  const platforms = [
    { id: 'all', name: '全部工具平台' },
    { id: 'claude', name: 'Claude Code' },
    { id: 'codex', name: 'Codex' },
    { id: 'gemini', name: 'Gemini CLI' },
  ];

  const currentPlatform = platforms.find(p => p.id === selectedTool) || platforms[0];

  return (
    <header className="h-16 px-4 bg-slate-950 border-b border-slate-800 flex items-center justify-between shrink-0 z-10 relative">
      <div className="flex items-center">
        {/* Mobile menu toggle */}
        <button 
          onClick={() => setIsMobileMenuOpen(true)} 
          className="md:hidden mr-3 p-2 -ml-2 text-slate-400 hover:text-white rounded-md"
        >
          <Menu size={20} />
        </button>
      </div>

      {/* Global Tool Selector */}
      <div className="relative">
        <button 
          onClick={() => setIsDropdownOpen(!isDropdownOpen)}
          className="flex items-center space-x-2 bg-slate-900 border border-slate-700 hover:border-cyan-500/50 px-4 py-2 rounded-lg text-sm font-medium text-slate-200 transition-colors shadow-sm"
        >
          <Monitor size={16} className="text-cyan-400" />
          <span>{currentPlatform.name}</span>
          <ChevronDown size={14} className="text-slate-500 ml-1" />
        </button>

        {isDropdownOpen && (
          <div className="absolute right-0 mt-2 w-48 bg-slate-900 border border-slate-700 rounded-lg shadow-xl overflow-hidden z-50">
            <div className="py-1">
              {platforms.map(platform => (
                <button
                  key={platform.id}
                  onClick={() => {
                    setSelectedTool(platform.id);
                    setIsDropdownOpen(false);
                  }}
                  className={`w-full text-left px-4 py-2 text-sm flex items-center justify-between ${
                    selectedTool === platform.id 
                      ? 'bg-cyan-900/20 text-cyan-400' 
                      : 'text-slate-300 hover:bg-slate-800'
                  }`}
                >
                  {platform.name}
                  {selectedTool === platform.id && <CheckCircle size={14} />}
                </button>
              ))}
            </div>
          </div>
        )}
      </div>
    </header>
  );
};

// --- MAIN APP ---
export default function App() {
  const [isSidebarOpen, setIsSidebarOpen] = useState(false);
  const [activeMenu, setActiveMenu] = useState('overview');
  const [globalSelectedTool, setGlobalSelectedTool] = useState('all');

  // Render view based on active menu
  const renderView = () => {
    switch (activeMenu) {
      case 'overview': return <OverviewView />;
      case 'mcp': return <MCPView />;
      case 'skills': return <SkillsView />;
      case 'tool-list': return <ToolsStatusView />;
      default: 
        return (
          <div className="flex flex-col items-center justify-center h-full text-slate-500 fade-in">
             <AlertCircle size={48} className="mb-4 opacity-20" />
             <p className="text-lg font-medium">模块 [{activeMenu}] 正在开发中</p>
             <p className="text-sm mt-2">请点击侧边栏的其他模块 (如 MCP管理, Skills, 概览)</p>
          </div>
        );
    }
  };

  return (
    <div className="flex h-screen bg-slate-950 text-slate-300 font-sans overflow-hidden">
      
      {/* Mobile Sidebar Overlay */}
      {isSidebarOpen && (
        <div 
          className="fixed inset-0 bg-black/60 z-40 md:hidden backdrop-blur-sm"
          onClick={() => setIsSidebarOpen(false)}
        />
      )}

      {/* Sidebar Component */}
      <Sidebar 
        isOpen={isSidebarOpen} 
        setIsOpen={setIsSidebarOpen}
        activeMenu={activeMenu}
        setActiveMenu={(id) => { setActiveMenu(id); setIsSidebarOpen(false); }}
      />

      {/* Main Content Area */}
      <div className="flex-1 flex flex-col min-w-0 overflow-hidden relative z-0">
        
        {/* Global Topbar with Tool Selector */}
        <GlobalTopbar 
          selectedTool={globalSelectedTool}
          setSelectedTool={setGlobalSelectedTool}
          isMobileMenuOpen={isSidebarOpen}
          setIsMobileMenuOpen={setIsSidebarOpen}
        />

        {/* View Container */}
        <main className="flex-1 overflow-hidden bg-slate-950">
           {renderView()}
        </main>
      </div>

      {/* Global Styles for Animations and Scrollbar inside component for simplicity */}
      <style dangerouslySetInnerHTML={{__html: `
        .fade-in { animation: fadeIn 0.3s ease-in-out; }
        @keyframes fadeIn { from { opacity: 0; transform: translateY(5px); } to { opacity: 1; transform: translateY(0); } }
        
        .custom-scrollbar::-webkit-scrollbar { width: 6px; height: 6px; }
        .custom-scrollbar::-webkit-scrollbar-track { background: transparent; }
        .custom-scrollbar::-webkit-scrollbar-thumb { background: #334155; border-radius: 3px; }
        .custom-scrollbar::-webkit-scrollbar-thumb:hover { background: #475569; }
      `}} />
    </div>
  );
}